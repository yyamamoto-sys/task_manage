# モジュール H：グラフ（ラボ）

> PJ・ToDo・タスク・メンバーの関係性を可視化する実験的ビュー。
> 全体像は [`docs/dev/module-map.md`](../../../docs/dev/module-map.md)「H グラフ」。

## 主なファイル
| ファイル | 役割 |
|---|---|
| `GraphView.tsx` | Canvas＋自前の物理シミュレーションで関係グラフを描画。サイドバーの「ラボ」から起動 |

## 改修・バグ探しの注意点
- 読み取り専用ビュー（`appStore` から読むだけ・書き込みなし）。
- ラボ機能＝実験的。本体機能への依存を増やさない（低結合を保つ）。
